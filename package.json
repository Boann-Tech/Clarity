{
  "name": "clarity",
  "version": "0.1.0",
  "description": "Evidence for what you read. A Chrome extension that checks factual claims against authoritative sources — so you can spot misinformation and make up your own mind.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "node scripts/build.mjs",
    "icons": "node scripts/generate-icons.mjs"
  },
  "keywords": [],
  "devDependencies": {
    "@types/chrome": "^0.0.310",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}